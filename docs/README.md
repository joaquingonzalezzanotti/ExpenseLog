# Docs

- `palette-preview.html`: preview local de paletas UI.
- `screenshots/`: capturas del producto para README y comunicacion.
- `product-redesign-plan.md`: plan base de producto, arquitectura y redisenio funcional (fuente de verdad para trabajar en `dev`).
- `routing-architecture.md`: contrato tecnico de rutas (`/`, `/app/*`, `/api/*`), compatibilidad legacy y rewrites sugeridos.
- `railway-paid-migration.md`: guia de migracion a plan pago en Railway y retiro seguro del flujo temporal `/app/wake`.
