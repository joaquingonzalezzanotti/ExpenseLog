apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: expenselog-pvc
  namespace: expenselog
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi

